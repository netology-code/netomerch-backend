import React from 'react';

const styles = {
  height: '300px',
  textAlign: 'center',
  fontSize: '30px',
  marginTop: '50px',
};

const Loader = () => (
  <div style={styles}>Loading...</div>
);

export default Loader;
